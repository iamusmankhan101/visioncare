# Production Environment Variables
PORT=5004
NODE_ENV=production

# Database (Railway will handle SQLite file persistence)
DB_PATH=./products.db

# CORS Origins (add your deployed frontend URL)
CORS_ORIGIN=*
